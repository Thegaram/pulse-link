# Local runtime override values used by `npm run gen:config`
# This file is safe to commit; keep real values in `.env` (ignored by git).

TRANSPORT_MODE=pubsub
SIGNALING_BACKEND=ably

ABLY_KEY=
ABLY_TOKEN=
ABLY_CLIENT_ID=

SUPABASE_URL=
SUPABASE_ANON_KEY=

APP_VERSION=
